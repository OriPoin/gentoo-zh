diff --git a/cmake/reproc.cmake b/cmake/reproc.cmake
index d0efafc..29d7c2a 100644
--- a/cmake/reproc.cmake
+++ b/cmake/reproc.cmake
@@ -255,23 +255,41 @@ function(reproc_library TARGET LANGUAGE)
     )
 
     # Library
-
-    install(
-      TARGETS ${TARGET}
-      EXPORT ${TARGET}-targets
-      RUNTIME
-        DESTINATION ${CMAKE_INSTALL_BINDIR}
-        COMPONENT ${TARGET}-runtime
-      LIBRARY
-        DESTINATION ${CMAKE_INSTALL_LIBDIR}
-        COMPONENT ${TARGET}-runtime
-        NAMELINK_COMPONENT ${TARGET}-development
-      ARCHIVE
-        DESTINATION ${CMAKE_INSTALL_LIBDIR}
-        COMPONENT ${TARGET}-development
-      INCLUDES
-        DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
-    )
+    if(BUILD_SHARED_LIBS)
+      install(
+        TARGETS ${TARGET}
+        EXPORT ${TARGET}-targets
+        RUNTIME
+          DESTINATION ${CMAKE_INSTALL_BINDIR}
+          COMPONENT ${TARGET}-runtime
+        LIBRARY
+          DESTINATION ${CMAKE_INSTALL_LIBDIR}
+          COMPONENT ${TARGET}-runtime
+          NAMELINK_COMPONENT ${TARGET}-development
+        ARCHIVE
+          DESTINATION ${CMAKE_INSTALL_LIBDIR}
+          COMPONENT ${TARGET}-development
+        INCLUDES
+          DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
+      )
+    else()
+      install(
+        TARGETS ${TARGET}
+        EXPORT ${TARGET}-static-targets
+        RUNTIME
+          DESTINATION ${CMAKE_INSTALL_BINDIR}
+          COMPONENT ${TARGET}-runtime
+        LIBRARY
+          DESTINATION ${CMAKE_INSTALL_LIBDIR}
+          COMPONENT ${TARGET}-runtime
+          NAMELINK_COMPONENT ${TARGET}-development
+        ARCHIVE
+          DESTINATION ${CMAKE_INSTALL_LIBDIR}
+          COMPONENT ${TARGET}-development
+        INCLUDES
+          DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
+      )
+    endif()
 
     if(NOT APPLE)
       set_property(
@@ -300,12 +318,19 @@ function(reproc_library TARGET LANGUAGE)
       DESTINATION ${REPROC_INSTALL_CMAKECONFIGDIR}/${TARGET}
       COMPONENT ${TARGET}-development
     )
-
+    if(BUILD_SHARED_LIBS)
+      install(
+        EXPORT ${TARGET}-targets
+        DESTINATION ${REPROC_INSTALL_CMAKECONFIGDIR}/${TARGET}
+        COMPONENT ${TARGET}-development
+      )
+    else()
     install(
-      EXPORT ${TARGET}-targets
+      EXPORT ${TARGET}-static-targets
       DESTINATION ${REPROC_INSTALL_CMAKECONFIGDIR}/${TARGET}
       COMPONENT ${TARGET}-development
     )
+    endif()
 
     # pkg-config
 
